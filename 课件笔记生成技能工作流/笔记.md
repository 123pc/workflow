---
name: course-notes
description: 将课程原始课件（PDF/PPTX/DOCX）转化为结构化笔记。支持按章节整理（每章一个笔记文件）或汇总整理（一个总文件）。当用户提到"整理课件笔记"、"生成课程笔记"、"课件转笔记"、"提取课件内容"时触发此技能。
---

# 笔记 — 课件笔记生成器

## 概述

本技能将课程原始课件转化为结构化、图文并茂的学习笔记（Markdown 格式）。支持两种模式：

- **按章节整理**：同一章节的课件合并为一个笔记文件，每章独立
- **汇总整理**：所有课件整合为一份完整笔记

核心能力：文本提取、图片引用、公式还原（LaTeX）、推导补全、关键图表识图。

---

## Phase 1: 信息收集与范围确认

### 1.1 确认课件位置

向用户询问课件文件所在的目录路径。如果用户没有明确提供，请用户将课件文件（.pdf/.pptx/.docx）放入一个目录中。

### 1.2 扫描课件文件

使用 Glob 或 Bash(`ls`) 扫描该目录，列出所有 `.pdf`、`.pptx`、`.docx` 文件。以表格形式呈现给用户，包含文件名和大小。

### 1.3 确认整理模式与范围

向用户逐项确认以下信息：

1. **整理模式**：按章节整理（每章一个 .md 文件）还是汇总整理（一个总 .md 文件）？
2. **考试范围**：哪些章节/课件在考试范围内？哪些明确不在？
3. **章节分组**（按章节模式时）：哪些课件属于同一章节？如果文件名已包含章节号，自动识别并让用户确认。
4. **输出语言**：中文 / 英文 / 中英双语？
5. **输出文件名**：
   - 按章节模式：每章的 .md 文件名（可提供命名模板，如 `Chapter{N}_笔记.md`）
   - 汇总模式：单个 .md 文件名（如 `固体力学_笔记.md`）

### 1.4 创建输出目录

在课件所在目录下创建 `extracted_text/` 和 `extracted_images/` 两个子目录（如已存在则跳过）。

---

## Phase 2: 课件内容提取（文本 + 图片）

### 2.1 提供提取脚本

向用户说明：需要先用脚本把课件中的文本和图片同时提取出来。让用户将下方脚本保存为 `extract_course_materials.py` 放在课件目录下，修改配置区路径后运行。

脚本功能：
- **文本提取**：将所有页面的文字提取为 `.txt` 文件，存入 `extracted_text/`
- **图片提取**：将课件内嵌的所有图片导出，按 `{课件名}_p{页码}_img{序号}.{格式}` 命名，存入 `extracted_images/`
- **占位标记**：在文本中插入 `[图片: xxx.png]` 标记，精确记录每张图片在课件中的位置
- **依赖自检**：启动时检测 `pypdf`/`python-pptx`/`python-docx` 是否安装，缺失则给出安装命令

```python
"""
课程课件内容提取工具 —— 文本 + 图片
支持格式: .pdf (pypdf), .pptx (python-pptx), .docx (python-docx + zipfile)
所有依赖均为纯Python库，pip一键安装，无C编译依赖。

使用方法:
  1. 修改下方"配置区"的 课件目录、文本输出目录、图片输出目录
  2. 运行: python extract_course_materials.py
"""

import os
import sys
import zipfile
from pathlib import Path

# ============================================================
# 0. 依赖自检
# ============================================================
MISSING = []
try:
    from pypdf import PdfReader
except ImportError:
    MISSING.append("pypdf")
try:
    from pptx import Presentation
    from pptx.enum.shapes import MSO_SHAPE_TYPE
except ImportError:
    MISSING.append("python-pptx")
try:
    from docx import Document
except ImportError:
    MISSING.append("python-docx")

if MISSING:
    print("=" * 50)
    print("以下 Python 库未安装，请先运行：")
    print(f"  pip install {' '.join(MISSING)}")
    print("=" * 50)
    sys.exit(1)

# ============================================================
# 1. 配置区 —— 用户根据需要修改以下变量
# ============================================================
课件目录 = r"课件"               # 课件所在目录路径
文本输出目录 = r"extracted_text"   # 提取文本输出目录
图片输出目录 = r"extracted_images" # 提取图片输出目录

os.makedirs(文本输出目录, exist_ok=True)
os.makedirs(图片输出目录, exist_ok=True)

# ============================================================
# 2. 工具函数
# ============================================================

def _content_type_to_ext(content_type):
    """将 MIME content-type 映射为文件扩展名"""
    mapping = {
        "image/png":  ".png",
        "image/jpeg": ".jpg",
        "image/gif":  ".gif",
        "image/bmp":  ".bmp",
        "image/tiff": ".tiff",
        "image/webp": ".webp",
        "image/x-png": ".png",
        "image/x-emf": ".emf",
        "image/x-wmf": ".wmf",
    }
    return mapping.get(content_type, ".png")


def _guess_ext_from_bytes(data):
    """通过文件头魔术字节推断图片扩展名"""
    if data[:4] == b'\x89PNG':
        return '.png'
    if data[:2] == b'\xff\xd8':
        return '.jpg'
    if data[:4] == b'GIF8':
        return '.gif'
    if data[:2] == b'BM':
        return '.bmp'
    if data[:4] == b'RIFF' and data[8:12] == b'WEBP':
        return '.webp'
    return '.png'


def _sanitize_filename(s):
    """移除文件名中的非法字符"""
    illegal = '<>:"/\\|?*'
    for c in illegal:
        s = s.replace(c, '_')
    return s


# ============================================================
# 3. 格式提取函数 (文本 + 图片)
# ============================================================

def extract_pdf(filepath, basename):
    """提取 PDF 文本和图片"""
    reader = PdfReader(filepath)
    lines = []
    img_count = 0

    for i, page in enumerate(reader.pages):
        page_num = i + 1

        # --- 文本 ---
        text = page.extract_text()
        if text and text.strip():
            lines.append(f"\n--- 第{page_num}页 ---")
            lines.append(text)

        # --- 图片 ---
        try:
            for j, img_obj in enumerate(page.images):
                img_data = img_obj.data
                if not img_data or len(img_data) < 100:
                    continue
                ext = _guess_ext_from_bytes(img_data)
                img_name = f"{basename}_p{page_num}_img{j+1}{ext}"
                img_path = os.path.join(图片输出目录, img_name)
                with open(img_path, "wb") as f:
                    f.write(img_data)
                lines.append(f"[图片: {img_name}]")
                img_count += 1
        except Exception:
            pass

    return "\n".join(lines), len(reader.pages), img_count


def extract_pptx(filepath, basename):
    """提取 PPTX 文本和图片"""
    prs = Presentation(filepath)
    lines = []
    img_count = 0

    for i, slide in enumerate(prs.slides):
        slide_num = i + 1
        slide_texts = []

        for shape in slide.shapes:
            # --- 图片 ---
            if shape.shape_type == MSO_SHAPE_TYPE.PICTURE:
                try:
                    img_blob = shape.image.blob
                    if img_blob and len(img_blob) >= 100:
                        ext = _content_type_to_ext(shape.image.content_type)
                        img_name = f"{basename}_s{slide_num}_img{img_count+1}{ext}"
                        img_path = os.path.join(图片输出目录, img_name)
                        with open(img_path, "wb") as f:
                            f.write(img_blob)
                        slide_texts.append(f"[图片: {img_name}]")
                        img_count += 1
                except Exception:
                    pass

            # --- 文本框 ---
            if shape.has_text_frame:
                for para in shape.text_frame.paragraphs:
                    t = para.text.strip()
                    if t:
                        slide_texts.append(t)

            # --- 表格 ---
            if shape.has_table:
                table = shape.table
                for row in table.rows:
                    row_texts = []
                    for cell in row.cells:
                        ct = cell.text.strip().replace("\n", " ")
                        row_texts.append(ct)
                    slide_texts.append(" | ".join(row_texts))

        if slide_texts:
            lines.append(f"\n--- 第{slide_num}张幻灯片 ---")
            lines.append("\n".join(slide_texts))

    return "\n".join(lines), len(prs.slides), img_count


def extract_docx(filepath, basename):
    """提取 DOCX 文本和图片 (图片通过 zipfile 从 word/media/ 提取)"""
    doc = Document(filepath)
    lines = []
    img_count = 0

    # --- 文本 (通过 python-docx) ---
    for para in doc.paragraphs:
        t = para.text.strip()
        if t:
            lines.append(t)

    for ti, table in enumerate(doc.tables):
        lines.append(f"\n[表格 {ti+1}]")
        for row in table.rows:
            row_texts = []
            for cell in row.cells:
                ct = cell.text.strip().replace("\n", " ")
                row_texts.append(ct)
            lines.append(" | ".join(row_texts))

    # --- 图片 (通过 zipfile 从 docx 内部提取) ---
    try:
        with zipfile.ZipFile(filepath) as z:
            media_files = [n for n in z.namelist() if n.startswith("word/media/")]
            for idx, media_path in enumerate(media_files):
                img_data = z.read(media_path)
                if not img_data or len(img_data) < 100:
                    continue
                original_name = os.path.basename(media_path)
                ext = os.path.splitext(original_name)[1].lower()
                if ext not in ('.png', '.jpg', '.jpeg', '.gif', '.bmp', '.tiff', '.webp'):
                    ext = _guess_ext_from_bytes(img_data)
                img_name = f"{basename}_img{idx+1}{ext}"
                img_path = os.path.join(图片输出目录, img_name)
                with open(img_path, "wb") as f:
                    f.write(img_data)
                lines.append(f"[图片: {img_name}]")
                img_count += 1
    except Exception:
        pass

    return "\n".join(lines), len(doc.paragraphs), img_count


# ============================================================
# 4. 扫描并提取
# ============================================================
HANDLERS = {
    ".pdf":  extract_pdf,
    ".pptx": extract_pptx,
    ".docx": extract_docx,
}

目标文件 = []
for fname in sorted(os.listdir(课件目录)):
    ext = os.path.splitext(fname)[1].lower()
    if ext in HANDLERS:
        目标文件.append(fname)

if not 目标文件:
    print(f"在 '{课件目录}' 中未找到 .pdf/.pptx/.docx 文件，请检查路径。")
    sys.exit(1)

print(f"找到 {len(目标文件)} 个课件文件，开始提取文本和图片...\n")

总图片数 = 0
成功 = 0
失败 = []
空内容 = []

for idx, fname in enumerate(目标文件, 1):
    filepath = os.path.join(课件目录, fname)
    ext = os.path.splitext(fname)[1].lower()
    basename = _sanitize_filename(Path(fname).stem)
    handler = HANDLERS[ext]

    try:
        text, page_count, img_count = handler(filepath, basename)
        header = f"======= {fname} =======\n页数/幻灯片数: {page_count}\n" + "=" * 60

        if text.strip():
            outpath = os.path.join(文本输出目录, fname + ".txt")
            with open(outpath, "w", encoding="utf-8") as f:
                f.write(header + "\n" + text)
            成功 += 1
            总图片数 += img_count
            img_info = f", {img_count}张图片" if img_count else ""
            print(f"  ✓ [{成功}/{len(目标文件)}] {fname} ({page_count}p{img_info})")
        else:
            空内容.append(fname)
            print(f"  ⚠ [{成功}/{len(目标文件)}] {fname} — 提取内容为空")
    except Exception as e:
        失败.append((fname, str(e)))
        print(f"  ✗ [{成功}/{len(目标文件)}] {fname} — 错误: {e}")

# ============================================================
# 5. 结果报告
# ============================================================
print(f"\n{'=' * 50}")
print(f"提取完成: 成功 {成功}/{len(目标文件)}, 共提取 {总图片数} 张图片")
print(f"  文本目录: {os.path.abspath(文本输出目录)}")
print(f"  图片目录: {os.path.abspath(图片输出目录)}")

if 空内容:
    print(f"\n⚠ 以下文件提取内容为空 ({len(空内容)}个):")
    for fn in 空内容:
        print(f"  - {fn}")
    print("可能原因：PDF是扫描版（图片） / PPTX/DOCX只含图片不含文字。")
    print("建议：对于扫描版PDF，直接让AI助手读取PDF图片进行识别。")

if 失败:
    print(f"\n✗ 以下文件提取失败 ({len(失败)}个):")
    for fn, err in 失败:
        print(f"  - {fn}: {err}")

if not 空内容 and not 失败:
    print("所有文件提取成功！")
```

### 2.2 运行提取

让用户运行 `python extract_course_materials.py`。

如果提取已完成（`extracted_text/` 和 `extracted_images/` 已存在内容），直接跳过此步。

### 2.3 读取提取结果

提取成功后：

1. **逐一读取** `extracted_text/` 目录下属于当前章节/范围的所有 .txt 文件
2. **浏览图片** `extracted_images/` 目录，了解有哪些可用的图表资源
3. 对于内容较长的课件（>2000行），使用分段读取确保不遗漏

---

## Phase 3: 生成笔记

### 3.1 生成原则

**在动笔之前**，先思考以下问题：

1. 本章/课程的核心主线是什么？
2. 哪些概念是"基石"（后续概念依赖它）？
3. 哪些公式/定理是最重要的？
4. 学生在哪些地方最容易混淆？
5. **哪些图片是关键图表**？（核心示意图、公式推导图、流程图——优先引用）
6. **哪些公式在提取文本中可能损坏**？（逐一核对，用识图工具辅助还原）
7. **哪些推导步骤被省略了**？（补全被跳过的逻辑）

### 3.2 识图策略

当主模型不支持多模态时，使用可用的识图工具（如 MiniMax MCP `understand_image`）读取关键图片。

**优先识图的类型**：
- 核心概念示意图（系统框图、流程图、物理模型图）
- 关键公式图解（几何解释、坐标变换图）
- 对比图（正确vs错误、变换前vs变换后）
- 解题流程图

**不需要识图的类型**：
- 纯装饰性图片
- 与文字内容完全重复的简单公式截图
- 分辨率过低无法辨认的图片

**图片引用方式**：在 Markdown 中使用相对路径

```markdown
![描述文字](extracted_images/课件名_p3_img1.png)
```

### 3.3 公式还原与推导补全

#### 3.3.1 公式完整性检查

常见公式提取问题及修复：

| 问题类型 | 表现 | 修复方式 |
|---------|------|---------|
| 公式断裂 | 多行公式被拆成碎片、上下标错位 | 根据数学常识还原为完整 LaTeX |
| 符号丢失 | 积分号∫、求和号∑、希腊字母变成乱码 | 根据上下文推断正确符号 |
| 分数线丢失 | `a/b` 本应是 `\frac{a}{b}` | 识别并重写为标准 LaTeX 分数形式 |
| 上下标错位 | `x2` 本应是 `x^2` 或 `x_2` | 根据公式含义判断正确的上下标位置 |
| 特殊函数名混淆 | `sin`、`cos`、`lim` 未正确格式化 | 统一写为 `\sin`、`\cos`、`\lim` |

#### 3.3.2 推导步骤补全

课件中常见的省略（"易证""显然""由此可得"）必须补全。

**补全格式**：

```markdown
📐 **推导**: [定理名/公式名]

从 [起点] 出发：

**第1步**：[操作描述——做了什么、为什么这样做]
$$[中间表达式1]$$

**第2步**：[操作描述]
$$[中间表达式2]$$

**最终结果**：
$$\boxed{[最终公式]}$$

> 💡 **推导要点**：[最关键的技巧或最容易出错的地方]
```

**补全优先级**：

| 优先级 | 范围 | 说明 |
|--------|------|------|
| 🔴 必须补全 | 核心公式的推导 | 课件中常被"易证"跳过 |
| 🔴 必须补全 | 从基本定义到核心定理的推导 | 课程逻辑的主干 |
| 🟡 建议补全 | 例题中的计算步骤 | 课件常只给结果不给过程 |
| 🟢 可选 | 纯代数化简的每一步 | 不需要无限展开 |

### 3.4 笔记结构模板

#### 3.4.1 按章节模式（每章一个文件）

```markdown
# 第{N}章：[章节名]

> **课件来源**: [文件名列表]
> **考试范围**: [是/否]

---

## {N}.1 [小节名]

### 📌 [概念中文名] ([英文名])

[精确定义，1-2句话]

$$[核心公式]$$

![概念示意图](extracted_images/xxx.png)

**意义**: [这个概念在课程体系中的位置，解决什么问题]

> 💡 **直观理解**: [用日常比喻或通俗语言解释]

📐 **推导**: [如课件中推导有省略，在此补全]

✏️ **例题**: [题目+分步解答]

**关联**: [指向相关概念]

---

## {N}.2 [小节名]
...
```

#### 3.4.2 汇总模式（一个总文件）

```markdown
# [课程名] 笔记

> **整理范围**: [课件列表]
> **整理日期**: [日期]

---

## 目录

- [第N章：...](#)
- ...

---

## 第{N}章：[章节名]

（每章内部结构同上）
```

### 3.5 图标系统

| 图标 | 含义 | 用途 |
|------|------|------|
| 📌 | 核心概念/定义 | 标记每个必须掌握的概念 |
| 🔑 | 关键关系/定理 | 标记核心考点 |
| 📐 | 完整推导 | 标记补全了课件省略步骤的推导 |
| ✏️ | 计算例题 | 标记需要动手做的题目 |
| 💡 | 直观理解 | 标记通俗解释 |

### 3.6 质量确保标准

1. **双语精确性**：所有关键技术术语同时给出中文和英文
2. **数学排版**：所有公式使用标准 LaTeX 语法，重要结果用 `\boxed{}`
3. **公式完整性**：逐一核对每个公式——断裂的拼接完整、丢失的符号补回
4. **推导补全**：课件中所有被跳过的推导步骤必须补全，每步有文字解释
5. **图文并茂**：关键概念配有课件原图，每章至少 3 张图片
6. **学生同理心**：预判易混淆点，用对比表格澄清；用日常比喻建立直觉
7. **统一视觉语言**：章节标题 `## 第N章：[标题]`，小节 `### N.M [标题]`，推导 `📐 **推导**`，直觉 `> 💡` 块引用

### 3.7 输出

- **按章节模式**：每章一个 .md 文件，保存到课件目录或用户指定位置
- **汇总模式**：单个 .md 文件

输出后报告统计信息：章节数、核心概念数、公式数（近似）、补全推导数、引用图片数。

---

## 质量检查清单（生成后自检）

- [ ] 所有核心概念使用 📌 标记，关键定理使用 🔑 标记
- [ ] 所有数学公式使用 LaTeX，关键结果 \boxed{}
- [ ] 每个公式完整性已核对——无断裂、无符号丢失
- [ ] 课件中被跳过的推导步骤均已补全
- [ ] 每章至少有 3 张图片引用，路径正确
- [ ] 关键术语均为中英双语
- [ ] 各章节均标注对应课件来源
- [ ] 关键图表已通过识图工具读取并描述
